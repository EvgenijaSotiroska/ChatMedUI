apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend-app
spec:
  selector:
    app: frontend-app
  ports:
    - name: http
      port: 80         
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
